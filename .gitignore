# Zip Files

*.zip 

# Python

.Python
build/
develop-eggs/
dist/
.env
.venv
env/
venv/
__pycache__

# System

.DS_STORE

# LocalStack

volume/
.idea

# lstk runtime state (log + keyring lock). Per-project lstk config
# (.lstk/config.toml) IS checked in; everything else under .lstk/ is
# machine-local.
.lstk/*.log
.lstk/*.lock

cdk.context.json

# Claude Code local-only settings
.claude/settings.local.json

.vscode/
